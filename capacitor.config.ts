import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simplisticscientists',
  appName: 'Simplistic Scientists',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Simplistic Scientists',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
